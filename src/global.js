/* 存放全局变量 */
var _GLBConfig = {
    // game param
    GROUND_WIDTH: 3000,
    GROUND_HEIGHT: 2000,
    FOOD_INIT_COUNT: 200,
    FOOD_TRANSFER_COUNT: 10,

    // events
    GAME_START_EVENT: "ev_gameStart",
    FOOD_INIT_EVENT: "ev_foodInit"
};

module.exports = _GLBConfig;
